{{/* Twenty Server */}}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-twenty
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "b2b-sdr.labels" . | nindent 4 }}
    app.kubernetes.io/component: twenty
spec:
  replicas: 1
  selector:
    matchLabels:
      {{- include "b2b-sdr.selectorLabels" (dict "release" .Release.Name "component" "twenty") | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "b2b-sdr.selectorLabels" (dict "release" .Release.Name "component" "twenty") | nindent 8 }}
    spec:
      automountServiceAccountToken: false
      # fsGroup matches the 'node' user (UID 1000) in twentycrm/twenty image so the
      # PVC at /app/.local-storage is group-writable. Without this, signUpInNewWorkspace
      # fails with EACCES when Twenty tries to mkdir the workspace storage dir.
      imagePullSecrets:
        - name: registry-pull-secret
      securityContext:
        fsGroup: 1000
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      initContainers:
        # Wait for Postgres to be accepting connections before Twenty tries to migrate.
        - name: wait-for-postgres
          image: {{ .Values.busybox.image.repository }}:{{ .Values.busybox.image.tag }}
          imagePullPolicy: Always
          command: ["sh", "-c"]
          args:
            - |
              until nc -z {{ .Release.Name }}-postgres 5432; do
                echo "Waiting for postgres..."; sleep 3;
              done
              echo "Postgres ready."
          resources:
            requests:
              cpu: 10m
              memory: 16Mi
            limits:
              cpu: 50m
              memory: 32Mi
      containers:
        - name: twenty-server
          image: {{ .Values.twenty.image.repository }}:{{ .Values.twenty.image.tag }}
          imagePullPolicy: {{ .Values.twenty.image.pullPolicy }}
          ports:
            - name: http
              containerPort: 3000
              protocol: TCP
          env:
            - name: NODE_ENV
              value: production
            - name: PG_DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: {{ .Release.Name }}-secrets
                  key: TWENTY_PG_DSN
            - name: REDIS_URL
              value: redis://{{ .Release.Name }}-redis:6379
            - name: APP_SECRET
              valueFrom:
                secretKeyRef:
                  name: {{ .Release.Name }}-secrets
                  key: TWENTY_APP_SECRET
            {{- if .Values.twenty.serverUrl }}
            - name: SERVER_URL
              value: {{ .Values.twenty.serverUrl | quote }}
            {{- else }}
            - name: SERVER_URL
              value: {{ include "b2b-sdr.twentyInternalUrl" . | quote }}
            {{- end }}
            # Do NOT set ENABLE_DB_MIGRATIONS_ON_STARTUP — in Twenty v2.x that flag tells
            # the server to run migrations and then EXIT (migration-only job mode). The
            # entrypoint.sh handles migrations correctly via DISABLE_DB_MIGRATIONS (unset
            # here = run migrations). We let entrypoint.sh do its job and start the server.
            - name: IS_CONFIG_VARIABLES_IN_DB_ENABLED
              value: "false"
            - name: STORAGE_TYPE
              value: local
            - name: STORAGE_LOCAL_PATH
              value: /app/.local-storage
          resources:
            {{- toYaml .Values.twenty.resources | nindent 12 }}
          readinessProbe:
            httpGet:
              path: /healthz
              port: 3000
            # Twenty pulls 277MB image + runs migrations on first boot (~3min total).
            # Start probing at 180s; with failureThreshold=24 (240s more) we cover up to 7min total.
            initialDelaySeconds: 180
            periodSeconds: 10
            failureThreshold: 24
          livenessProbe:
            httpGet:
              path: /healthz
              port: 3000
            initialDelaySeconds: 480
            periodSeconds: 20
            timeoutSeconds: 5
            failureThreshold: 3
          volumeMounts:
            - name: local-storage
              mountPath: /app/.local-storage
      volumes:
        - name: local-storage
          persistentVolumeClaim:
            claimName: {{ .Release.Name }}-twenty-storage
---
{{/* Twenty Worker — background job processor */}}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-twenty-worker
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "b2b-sdr.labels" . | nindent 4 }}
    app.kubernetes.io/component: twenty-worker
spec:
  replicas: 1
  selector:
    matchLabels:
      {{- include "b2b-sdr.selectorLabels" (dict "release" .Release.Name "component" "twenty-worker") | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "b2b-sdr.selectorLabels" (dict "release" .Release.Name "component" "twenty-worker") | nindent 8 }}
    spec:
      automountServiceAccountToken: false
      imagePullSecrets:
        - name: registry-pull-secret
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      initContainers:
        # Wait for Twenty server to pass /healthz before starting the worker.
        - name: wait-for-twenty
          image: {{ .Values.busybox.image.repository }}:{{ .Values.busybox.image.tag }}
          imagePullPolicy: Always
          command: ["sh", "-c"]
          args:
            - |
              until wget -qO- http://{{ .Release.Name }}-twenty:3000/healthz >/dev/null 2>&1; do
                echo "Waiting for Twenty server..."; sleep 5;
              done
              echo "Twenty server ready."
          resources:
            requests:
              cpu: 10m
              memory: 16Mi
            limits:
              cpu: 50m
              memory: 32Mi
      containers:
        - name: twenty-worker
          image: {{ .Values.twenty.image.repository }}:{{ .Values.twenty.image.tag }}
          imagePullPolicy: {{ .Values.twenty.image.pullPolicy }}
          command: ["yarn", "worker:prod"]
          env:
            - name: NODE_ENV
              value: production
            - name: PG_DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: {{ .Release.Name }}-secrets
                  key: TWENTY_PG_DSN
            - name: REDIS_URL
              value: redis://{{ .Release.Name }}-redis:6379
            - name: APP_SECRET
              valueFrom:
                secretKeyRef:
                  name: {{ .Release.Name }}-secrets
                  key: TWENTY_APP_SECRET
            - name: IS_CONFIG_VARIABLES_IN_DB_ENABLED
              value: "false"
          resources:
            requests:
              cpu: 100m
              memory: 512Mi
            limits:
              cpu: "1"
              memory: 1Gi
