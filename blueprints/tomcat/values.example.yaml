# Apache Tomcat — example values
# helm install tomcat blueprints/tomcat -f my-values.yaml

tomcatUsername: user
tomcatPassword: "YOUR-MANAGER-PASSWORD"

service:
  type: NodePort

persistence:
  enabled: true
  size: 8Gi
  storageClass: ""

resources:
  requests:
    cpu: 250m
    memory: 256Mi
