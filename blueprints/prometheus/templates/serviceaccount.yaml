apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ .Release.Name }}-ingress-patcher
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "prom.labels" . | nindent 4 }}
---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: {{ .Release.Name }}-ingress-patcher
  namespace: {{ .Release.Namespace }}
rules:
  - apiGroups: ["networking.k8s.io"]
    resources: ["ingresses"]
    verbs: ["get", "patch"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: {{ .Release.Name }}-ingress-patcher
  namespace: {{ .Release.Namespace }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: {{ .Release.Name }}-ingress-patcher
subjects:
  - kind: ServiceAccount
    name: {{ .Release.Name }}-ingress-patcher
    namespace: {{ .Release.Namespace }}
