RAG Knowledge Base deployed successfully!

Components:
  - Open WebUI (chat + document upload): {{ .Release.Name }}-openwebui:8080
  - Ollama (embedding + generation):      {{ .Release.Name }}-ollama:11434
  - Qdrant (vector store):                {{ .Release.Name }}-qdrant:6333
  - n8n (URL ingestion automation):       {{ .Release.Name }}-n8n:5678

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
FIRST TIME SETUP (takes ~10-15 min on CPU node)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1. Wait for Ollama initContainer to finish pulling models:
   kubectl logs -n {{ .Release.Namespace }} deploy/{{ .Release.Name }}-ollama -c pull-models -f

2. Open WebUI will be available at the platform URL shown in the marketplace.
   Login with:  {{ .Values.openwebui.adminEmail | default "(email you provided)" }}
   Password:    {{ .Values.openwebui.adminPassword }}

3. In Open WebUI → Admin Panel → Documents:
   - Set Embedding Model: nomic-embed-text
   - Vector Database: Qdrant (auto-configured)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
USING THE RAG KNOWLEDGE BASE
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Upload documents via Open WebUI:
  Workspace → Knowledge → Create → Upload files or paste text

Ingest URLs via n8n webhook:
  curl -X POST http://{{ .Release.Name }}-n8n:5678/webhook/ingest \
       -H "Content-Type: application/json" \
       -d '{"url":"https://example.com/docs","collection":"my-kb"}'

  Note: Set OPENWEBUI_API_KEY env var in n8n deployment to enable API auth.
  Get your API key from Open WebUI → Settings → Account → API Keys.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
CHAT WITH YOUR DOCUMENTS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

In Open WebUI → New Chat → select model {{ .Values.ollama.models.generation }}
Use "#" to select a knowledge collection, then ask questions grounded in your docs.
