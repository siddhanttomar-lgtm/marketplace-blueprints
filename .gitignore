# Packaged Helm charts (root-level only — not vendored deps inside charts/)
/*.tgz
.cr-release-packages/*.tgz

# chart-releaser output
.cr-index/

# Claude Code local memory and instructions (not for public repo)
.claude/
CLAUDE.md

# Editor and OS files
.DS_Store
.idea/
*.swp
*.swo
*~

# Local values files with real credentials (root-level only)
**/my-values.yaml
**/local-values.yaml
/*-secret.yaml
blueprints/*/*-secret.yaml
!blueprints/
